# filesofpeople

Simple static "classified database" website with guest uploads, admin review queue, and archive viewing.

## Run locally

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.

## Admin credentials

- Username: `bilal`
- Password: `saadiboy`

## Included pages

- Home (`index.html`)
- Archive (`archive.html`)
- Upload (`upload.html`)
- File Viewer (`file.html`)
- Admin Login (`admin-login.html`)
- Admin Dashboard (`admin-dashboard.html`)
- Admin Submissions Review (`admin-review.html`)
